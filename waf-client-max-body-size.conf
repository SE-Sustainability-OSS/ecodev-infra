# Loaded at http scope via /etc/nginx/conf.d/*.conf in the owasp/modsecurity-crs:nginx image.
# Aligns nginx with the plugin maxBodySize and MODSEC_REQ_BODY_LIMIT (both 209715200 bytes),
# so large uploads are not rejected with a 413 before reaching the backend.
client_max_body_size 200m;
